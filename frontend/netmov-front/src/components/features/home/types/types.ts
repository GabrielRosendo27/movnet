export interface Segment {
  text: string;
  style: string;
  visibleText: string;
}
export interface PopularMovie {
  id: number;
  title: string;
  rating: number;
  posterPath: string;
  releaseDate: string;
}
